"use client";

export function MobileNav() {
  // Mobile nav is intentionally removed to allow the full-screen map 
  // and bottom-sheet UI to occupy the entire screen.
  // Navigation is now handled by the Top App Bar (Header).
  return null;
}
